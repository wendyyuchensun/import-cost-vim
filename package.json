{
  "name": "import-cost-vim",
  "version": "1.0.0",
  "description": "Displays import size of JavaScript packages inside Vim.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc --outDir dist --module commonjs --lib es2015 src/index.ts",
    "start": "npm run build && node ."
  },
  "keywords": [
    "import-cost",
    "vim"
  ],
  "author": "Wendy Yuchen Sun",
  "license": "ISC",
  "dependencies": {
    "import-cost": "^1.3.1"
  },
  "devDependencies": {
    "@types/node": "^9.4.6",
    "typescript": "^2.7.2"
  }
}
